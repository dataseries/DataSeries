IMPORTANT INFORMATION

This package is referred to as DataSeries.  It originates from the Storage
Systems Department (SSD, or SSP) of Hewlett-Packard Laboratories, Palo
Alto, CA 94304.  IT IS PROVIDED TO YOU SOLELY UNDER THE TERMS OF THE
AGREEMENTS DETAILED IN THE COPYING FILE.  If you do not agree to any of 
these terms, you must delete this software, and any copies you may have 
made of it.

COMPILATION:

1) You need to build and install Lintel.

2) If you have a checked out a copy of DataSeries, install autoconf and 
   libtool.  Then run: 
   % lintel-acinclude && autoreconf --install

3) configure the package, e.g. in ~/build/optimize/DataSeries:
   % .../path/to/source/DataSeries/configure --enable-optmode=optimize

4) build and install the package, e.g. in ~/build/optimize/DataSeries:
   % make && make check && make install
