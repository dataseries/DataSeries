INCLUDE(LintelCMakeUtil)

IF(LINTEL_LATEX_REBUILD_ENABLED AND BUILD_TECH_REPORT)
    LINTEL_LATEX(tr)
    INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/tr.pdf
            DESTINATION ${CMAKE_INSTALL_PREFIX}/share/doc/Lintel)
ENDIF(LINTEL_LATEX_REBUILD_ENABLED AND BUILD_TECH_REPORT)
